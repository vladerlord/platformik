# mise (toolchain management)

Tool versions are pinned in `.mise.toml`.

## Setup

1. `mise trust`
2. `mise install`

## Why mise

- Consistent tool versions across contributors and CI.
- Avoids reliance on system-installed Node/Python/Go.
