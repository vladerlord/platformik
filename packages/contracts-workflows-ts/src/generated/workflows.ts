// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v6.33.4
// source: workflows.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { UUID } from "@platformik/contracts-primitives-ts";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Struct } from "./google/protobuf/struct";

export const protobufPackage = "platformik.workflows.v1";

export enum NodeType {
  NODE_TYPE_UNSPECIFIED = 0,
  NODE_TYPE_START = 1,
  NODE_TYPE_OPTION_SELECTION = 2,
  NODE_TYPE_SEND_MESSAGE = 3,
  NODE_TYPE_END = 4,
  UNRECOGNIZED = -1,
}

export function nodeTypeFromJSON(object: any): NodeType {
  switch (object) {
    case 0:
    case "NODE_TYPE_UNSPECIFIED":
      return NodeType.NODE_TYPE_UNSPECIFIED;
    case 1:
    case "NODE_TYPE_START":
      return NodeType.NODE_TYPE_START;
    case 2:
    case "NODE_TYPE_OPTION_SELECTION":
      return NodeType.NODE_TYPE_OPTION_SELECTION;
    case 3:
    case "NODE_TYPE_SEND_MESSAGE":
      return NodeType.NODE_TYPE_SEND_MESSAGE;
    case 4:
    case "NODE_TYPE_END":
      return NodeType.NODE_TYPE_END;
    case -1:
    case "UNRECOGNIZED":
    default:
      return NodeType.UNRECOGNIZED;
  }
}

export function nodeTypeToJSON(object: NodeType): string {
  switch (object) {
    case NodeType.NODE_TYPE_UNSPECIFIED:
      return "NODE_TYPE_UNSPECIFIED";
    case NodeType.NODE_TYPE_START:
      return "NODE_TYPE_START";
    case NodeType.NODE_TYPE_OPTION_SELECTION:
      return "NODE_TYPE_OPTION_SELECTION";
    case NodeType.NODE_TYPE_SEND_MESSAGE:
      return "NODE_TYPE_SEND_MESSAGE";
    case NodeType.NODE_TYPE_END:
      return "NODE_TYPE_END";
    case NodeType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum WorkflowRunStatus {
  WORKFLOW_RUN_STATUS_UNSPECIFIED = 0,
  WORKFLOW_RUN_STATUS_RUNNING = 1,
  WORKFLOW_RUN_STATUS_COMPLETED = 2,
  WORKFLOW_RUN_STATUS_FAILED = 3,
  UNRECOGNIZED = -1,
}

export function workflowRunStatusFromJSON(object: any): WorkflowRunStatus {
  switch (object) {
    case 0:
    case "WORKFLOW_RUN_STATUS_UNSPECIFIED":
      return WorkflowRunStatus.WORKFLOW_RUN_STATUS_UNSPECIFIED;
    case 1:
    case "WORKFLOW_RUN_STATUS_RUNNING":
      return WorkflowRunStatus.WORKFLOW_RUN_STATUS_RUNNING;
    case 2:
    case "WORKFLOW_RUN_STATUS_COMPLETED":
      return WorkflowRunStatus.WORKFLOW_RUN_STATUS_COMPLETED;
    case 3:
    case "WORKFLOW_RUN_STATUS_FAILED":
      return WorkflowRunStatus.WORKFLOW_RUN_STATUS_FAILED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return WorkflowRunStatus.UNRECOGNIZED;
  }
}

export function workflowRunStatusToJSON(object: WorkflowRunStatus): string {
  switch (object) {
    case WorkflowRunStatus.WORKFLOW_RUN_STATUS_UNSPECIFIED:
      return "WORKFLOW_RUN_STATUS_UNSPECIFIED";
    case WorkflowRunStatus.WORKFLOW_RUN_STATUS_RUNNING:
      return "WORKFLOW_RUN_STATUS_RUNNING";
    case WorkflowRunStatus.WORKFLOW_RUN_STATUS_COMPLETED:
      return "WORKFLOW_RUN_STATUS_COMPLETED";
    case WorkflowRunStatus.WORKFLOW_RUN_STATUS_FAILED:
      return "WORKFLOW_RUN_STATUS_FAILED";
    case WorkflowRunStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum MessageRole {
  MESSAGE_ROLE_UNSPECIFIED = 0,
  MESSAGE_ROLE_USER = 1,
  MESSAGE_ROLE_SYSTEM = 2,
  UNRECOGNIZED = -1,
}

export function messageRoleFromJSON(object: any): MessageRole {
  switch (object) {
    case 0:
    case "MESSAGE_ROLE_UNSPECIFIED":
      return MessageRole.MESSAGE_ROLE_UNSPECIFIED;
    case 1:
    case "MESSAGE_ROLE_USER":
      return MessageRole.MESSAGE_ROLE_USER;
    case 2:
    case "MESSAGE_ROLE_SYSTEM":
      return MessageRole.MESSAGE_ROLE_SYSTEM;
    case -1:
    case "UNRECOGNIZED":
    default:
      return MessageRole.UNRECOGNIZED;
  }
}

export function messageRoleToJSON(object: MessageRole): string {
  switch (object) {
    case MessageRole.MESSAGE_ROLE_UNSPECIFIED:
      return "MESSAGE_ROLE_UNSPECIFIED";
    case MessageRole.MESSAGE_ROLE_USER:
      return "MESSAGE_ROLE_USER";
    case MessageRole.MESSAGE_ROLE_SYSTEM:
      return "MESSAGE_ROLE_SYSTEM";
    case MessageRole.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface WorkflowSummary {
  id: UUID | undefined;
  title: string;
}

export interface ConversationMessage {
  id: UUID | undefined;
  conversationId: UUID | undefined;
  runId: UUID | undefined;
  role: MessageRole;
  content: { [key: string]: any } | undefined;
}

export interface Option {
  id: string;
  label: string;
}

export interface OptionInput {
  label: string;
  options: Option[];
}

export interface PendingInput {
  optionInput?: OptionInput | undefined;
}

export interface ListWorkflowsRequest {
}

export interface ListWorkflowsResponse {
  workflows: WorkflowSummary[];
}

export interface StartWorkflowRequest {
  workflowId: UUID | undefined;
}

export interface StartWorkflowResponse {
  workflowRunId: UUID | undefined;
  conversationId: UUID | undefined;
  temporalWorkflowId: string;
}

export interface GetWorkflowRunViewRequest {
  workflowRunId:
    | UUID
    | undefined;
  /** absent = full history */
  afterId?: UUID | undefined;
}

export interface GetWorkflowRunViewResponse {
  conversationId: UUID | undefined;
  status: WorkflowRunStatus;
  currentNodeId: string;
  revision: number;
  /** absent if no messages yet */
  lastMessageId?: UUID | undefined;
  messages: ConversationMessage[];
  pendingInput?: PendingInput | undefined;
}

export interface SelectOptionAnswer {
  /** resolved option id — for clients that resolve locally (CLI, web) */
  optionId?:
    | string
    | undefined;
  /** raw user text — for clients that delegate resolution to the server (Telegram, SMS) */
  rawInput?: string | undefined;
}

export interface SubmitAnswerRequest {
  workflowRunId: UUID | undefined;
  selectOption?: SelectOptionAnswer | undefined;
}

export interface SubmitAnswerResponse {
}

function createBaseWorkflowSummary(): WorkflowSummary {
  return { id: undefined, title: "" };
}

export const WorkflowSummary: MessageFns<WorkflowSummary> = {
  encode(message: WorkflowSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      UUID.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = UUID.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowSummary {
    return {
      id: isSet(object.id) ? UUID.fromJSON(object.id) : undefined,
      title: isSet(object.title) ? globalThis.String(object.title) : "",
    };
  },

  toJSON(message: WorkflowSummary): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = UUID.toJSON(message.id);
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    return obj;
  },

  create(base?: DeepPartial<WorkflowSummary>): WorkflowSummary {
    return WorkflowSummary.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WorkflowSummary>): WorkflowSummary {
    const message = createBaseWorkflowSummary();
    message.id = (object.id !== undefined && object.id !== null) ? UUID.fromPartial(object.id) : undefined;
    message.title = object.title ?? "";
    return message;
  },
};

function createBaseConversationMessage(): ConversationMessage {
  return { id: undefined, conversationId: undefined, runId: undefined, role: 0, content: undefined };
}

export const ConversationMessage: MessageFns<ConversationMessage> = {
  encode(message: ConversationMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      UUID.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.conversationId !== undefined) {
      UUID.encode(message.conversationId, writer.uint32(18).fork()).join();
    }
    if (message.runId !== undefined) {
      UUID.encode(message.runId, writer.uint32(26).fork()).join();
    }
    if (message.role !== 0) {
      writer.uint32(32).int32(message.role);
    }
    if (message.content !== undefined) {
      Struct.encode(Struct.wrap(message.content), writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConversationMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConversationMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = UUID.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.conversationId = UUID.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.runId = UUID.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.role = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.content = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConversationMessage {
    return {
      id: isSet(object.id) ? UUID.fromJSON(object.id) : undefined,
      conversationId: isSet(object.conversationId)
        ? UUID.fromJSON(object.conversationId)
        : isSet(object.conversation_id)
        ? UUID.fromJSON(object.conversation_id)
        : undefined,
      runId: isSet(object.runId)
        ? UUID.fromJSON(object.runId)
        : isSet(object.run_id)
        ? UUID.fromJSON(object.run_id)
        : undefined,
      role: isSet(object.role) ? messageRoleFromJSON(object.role) : 0,
      content: isObject(object.content) ? object.content : undefined,
    };
  },

  toJSON(message: ConversationMessage): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = UUID.toJSON(message.id);
    }
    if (message.conversationId !== undefined) {
      obj.conversationId = UUID.toJSON(message.conversationId);
    }
    if (message.runId !== undefined) {
      obj.runId = UUID.toJSON(message.runId);
    }
    if (message.role !== 0) {
      obj.role = messageRoleToJSON(message.role);
    }
    if (message.content !== undefined) {
      obj.content = message.content;
    }
    return obj;
  },

  create(base?: DeepPartial<ConversationMessage>): ConversationMessage {
    return ConversationMessage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ConversationMessage>): ConversationMessage {
    const message = createBaseConversationMessage();
    message.id = (object.id !== undefined && object.id !== null) ? UUID.fromPartial(object.id) : undefined;
    message.conversationId = (object.conversationId !== undefined && object.conversationId !== null)
      ? UUID.fromPartial(object.conversationId)
      : undefined;
    message.runId = (object.runId !== undefined && object.runId !== null) ? UUID.fromPartial(object.runId) : undefined;
    message.role = object.role ?? 0;
    message.content = object.content ?? undefined;
    return message;
  },
};

function createBaseOption(): Option {
  return { id: "", label: "" };
}

export const Option: MessageFns<Option> = {
  encode(message: Option, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.label !== "") {
      writer.uint32(18).string(message.label);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Option {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOption();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.label = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Option {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      label: isSet(object.label) ? globalThis.String(object.label) : "",
    };
  },

  toJSON(message: Option): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.label !== "") {
      obj.label = message.label;
    }
    return obj;
  },

  create(base?: DeepPartial<Option>): Option {
    return Option.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Option>): Option {
    const message = createBaseOption();
    message.id = object.id ?? "";
    message.label = object.label ?? "";
    return message;
  },
};

function createBaseOptionInput(): OptionInput {
  return { label: "", options: [] };
}

export const OptionInput: MessageFns<OptionInput> = {
  encode(message: OptionInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.label !== "") {
      writer.uint32(10).string(message.label);
    }
    for (const v of message.options) {
      Option.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OptionInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOptionInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.label = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.options.push(Option.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OptionInput {
    return {
      label: isSet(object.label) ? globalThis.String(object.label) : "",
      options: globalThis.Array.isArray(object?.options) ? object.options.map((e: any) => Option.fromJSON(e)) : [],
    };
  },

  toJSON(message: OptionInput): unknown {
    const obj: any = {};
    if (message.label !== "") {
      obj.label = message.label;
    }
    if (message.options?.length) {
      obj.options = message.options.map((e) => Option.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<OptionInput>): OptionInput {
    return OptionInput.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OptionInput>): OptionInput {
    const message = createBaseOptionInput();
    message.label = object.label ?? "";
    message.options = object.options?.map((e) => Option.fromPartial(e)) || [];
    return message;
  },
};

function createBasePendingInput(): PendingInput {
  return { optionInput: undefined };
}

export const PendingInput: MessageFns<PendingInput> = {
  encode(message: PendingInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.optionInput !== undefined) {
      OptionInput.encode(message.optionInput, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PendingInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePendingInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.optionInput = OptionInput.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PendingInput {
    return {
      optionInput: isSet(object.optionInput)
        ? OptionInput.fromJSON(object.optionInput)
        : isSet(object.option_input)
        ? OptionInput.fromJSON(object.option_input)
        : undefined,
    };
  },

  toJSON(message: PendingInput): unknown {
    const obj: any = {};
    if (message.optionInput !== undefined) {
      obj.optionInput = OptionInput.toJSON(message.optionInput);
    }
    return obj;
  },

  create(base?: DeepPartial<PendingInput>): PendingInput {
    return PendingInput.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PendingInput>): PendingInput {
    const message = createBasePendingInput();
    message.optionInput = (object.optionInput !== undefined && object.optionInput !== null)
      ? OptionInput.fromPartial(object.optionInput)
      : undefined;
    return message;
  },
};

function createBaseListWorkflowsRequest(): ListWorkflowsRequest {
  return {};
}

export const ListWorkflowsRequest: MessageFns<ListWorkflowsRequest> = {
  encode(_: ListWorkflowsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListWorkflowsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListWorkflowsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListWorkflowsRequest {
    return {};
  },

  toJSON(_: ListWorkflowsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ListWorkflowsRequest>): ListWorkflowsRequest {
    return ListWorkflowsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ListWorkflowsRequest>): ListWorkflowsRequest {
    const message = createBaseListWorkflowsRequest();
    return message;
  },
};

function createBaseListWorkflowsResponse(): ListWorkflowsResponse {
  return { workflows: [] };
}

export const ListWorkflowsResponse: MessageFns<ListWorkflowsResponse> = {
  encode(message: ListWorkflowsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.workflows) {
      WorkflowSummary.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListWorkflowsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListWorkflowsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflows.push(WorkflowSummary.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListWorkflowsResponse {
    return {
      workflows: globalThis.Array.isArray(object?.workflows)
        ? object.workflows.map((e: any) => WorkflowSummary.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListWorkflowsResponse): unknown {
    const obj: any = {};
    if (message.workflows?.length) {
      obj.workflows = message.workflows.map((e) => WorkflowSummary.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListWorkflowsResponse>): ListWorkflowsResponse {
    return ListWorkflowsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListWorkflowsResponse>): ListWorkflowsResponse {
    const message = createBaseListWorkflowsResponse();
    message.workflows = object.workflows?.map((e) => WorkflowSummary.fromPartial(e)) || [];
    return message;
  },
};

function createBaseStartWorkflowRequest(): StartWorkflowRequest {
  return { workflowId: undefined };
}

export const StartWorkflowRequest: MessageFns<StartWorkflowRequest> = {
  encode(message: StartWorkflowRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workflowId !== undefined) {
      UUID.encode(message.workflowId, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartWorkflowRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartWorkflowRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowId = UUID.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartWorkflowRequest {
    return {
      workflowId: isSet(object.workflowId)
        ? UUID.fromJSON(object.workflowId)
        : isSet(object.workflow_id)
        ? UUID.fromJSON(object.workflow_id)
        : undefined,
    };
  },

  toJSON(message: StartWorkflowRequest): unknown {
    const obj: any = {};
    if (message.workflowId !== undefined) {
      obj.workflowId = UUID.toJSON(message.workflowId);
    }
    return obj;
  },

  create(base?: DeepPartial<StartWorkflowRequest>): StartWorkflowRequest {
    return StartWorkflowRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StartWorkflowRequest>): StartWorkflowRequest {
    const message = createBaseStartWorkflowRequest();
    message.workflowId = (object.workflowId !== undefined && object.workflowId !== null)
      ? UUID.fromPartial(object.workflowId)
      : undefined;
    return message;
  },
};

function createBaseStartWorkflowResponse(): StartWorkflowResponse {
  return { workflowRunId: undefined, conversationId: undefined, temporalWorkflowId: "" };
}

export const StartWorkflowResponse: MessageFns<StartWorkflowResponse> = {
  encode(message: StartWorkflowResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workflowRunId !== undefined) {
      UUID.encode(message.workflowRunId, writer.uint32(10).fork()).join();
    }
    if (message.conversationId !== undefined) {
      UUID.encode(message.conversationId, writer.uint32(18).fork()).join();
    }
    if (message.temporalWorkflowId !== "") {
      writer.uint32(26).string(message.temporalWorkflowId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartWorkflowResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartWorkflowResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowRunId = UUID.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.conversationId = UUID.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.temporalWorkflowId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartWorkflowResponse {
    return {
      workflowRunId: isSet(object.workflowRunId)
        ? UUID.fromJSON(object.workflowRunId)
        : isSet(object.workflow_run_id)
        ? UUID.fromJSON(object.workflow_run_id)
        : undefined,
      conversationId: isSet(object.conversationId)
        ? UUID.fromJSON(object.conversationId)
        : isSet(object.conversation_id)
        ? UUID.fromJSON(object.conversation_id)
        : undefined,
      temporalWorkflowId: isSet(object.temporalWorkflowId)
        ? globalThis.String(object.temporalWorkflowId)
        : isSet(object.temporal_workflow_id)
        ? globalThis.String(object.temporal_workflow_id)
        : "",
    };
  },

  toJSON(message: StartWorkflowResponse): unknown {
    const obj: any = {};
    if (message.workflowRunId !== undefined) {
      obj.workflowRunId = UUID.toJSON(message.workflowRunId);
    }
    if (message.conversationId !== undefined) {
      obj.conversationId = UUID.toJSON(message.conversationId);
    }
    if (message.temporalWorkflowId !== "") {
      obj.temporalWorkflowId = message.temporalWorkflowId;
    }
    return obj;
  },

  create(base?: DeepPartial<StartWorkflowResponse>): StartWorkflowResponse {
    return StartWorkflowResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StartWorkflowResponse>): StartWorkflowResponse {
    const message = createBaseStartWorkflowResponse();
    message.workflowRunId = (object.workflowRunId !== undefined && object.workflowRunId !== null)
      ? UUID.fromPartial(object.workflowRunId)
      : undefined;
    message.conversationId = (object.conversationId !== undefined && object.conversationId !== null)
      ? UUID.fromPartial(object.conversationId)
      : undefined;
    message.temporalWorkflowId = object.temporalWorkflowId ?? "";
    return message;
  },
};

function createBaseGetWorkflowRunViewRequest(): GetWorkflowRunViewRequest {
  return { workflowRunId: undefined, afterId: undefined };
}

export const GetWorkflowRunViewRequest: MessageFns<GetWorkflowRunViewRequest> = {
  encode(message: GetWorkflowRunViewRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workflowRunId !== undefined) {
      UUID.encode(message.workflowRunId, writer.uint32(10).fork()).join();
    }
    if (message.afterId !== undefined) {
      UUID.encode(message.afterId, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetWorkflowRunViewRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetWorkflowRunViewRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowRunId = UUID.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.afterId = UUID.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetWorkflowRunViewRequest {
    return {
      workflowRunId: isSet(object.workflowRunId)
        ? UUID.fromJSON(object.workflowRunId)
        : isSet(object.workflow_run_id)
        ? UUID.fromJSON(object.workflow_run_id)
        : undefined,
      afterId: isSet(object.afterId)
        ? UUID.fromJSON(object.afterId)
        : isSet(object.after_id)
        ? UUID.fromJSON(object.after_id)
        : undefined,
    };
  },

  toJSON(message: GetWorkflowRunViewRequest): unknown {
    const obj: any = {};
    if (message.workflowRunId !== undefined) {
      obj.workflowRunId = UUID.toJSON(message.workflowRunId);
    }
    if (message.afterId !== undefined) {
      obj.afterId = UUID.toJSON(message.afterId);
    }
    return obj;
  },

  create(base?: DeepPartial<GetWorkflowRunViewRequest>): GetWorkflowRunViewRequest {
    return GetWorkflowRunViewRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetWorkflowRunViewRequest>): GetWorkflowRunViewRequest {
    const message = createBaseGetWorkflowRunViewRequest();
    message.workflowRunId = (object.workflowRunId !== undefined && object.workflowRunId !== null)
      ? UUID.fromPartial(object.workflowRunId)
      : undefined;
    message.afterId = (object.afterId !== undefined && object.afterId !== null)
      ? UUID.fromPartial(object.afterId)
      : undefined;
    return message;
  },
};

function createBaseGetWorkflowRunViewResponse(): GetWorkflowRunViewResponse {
  return {
    conversationId: undefined,
    status: 0,
    currentNodeId: "",
    revision: 0,
    lastMessageId: undefined,
    messages: [],
    pendingInput: undefined,
  };
}

export const GetWorkflowRunViewResponse: MessageFns<GetWorkflowRunViewResponse> = {
  encode(message: GetWorkflowRunViewResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.conversationId !== undefined) {
      UUID.encode(message.conversationId, writer.uint32(10).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.currentNodeId !== "") {
      writer.uint32(26).string(message.currentNodeId);
    }
    if (message.revision !== 0) {
      writer.uint32(32).uint64(message.revision);
    }
    if (message.lastMessageId !== undefined) {
      UUID.encode(message.lastMessageId, writer.uint32(42).fork()).join();
    }
    for (const v of message.messages) {
      ConversationMessage.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.pendingInput !== undefined) {
      PendingInput.encode(message.pendingInput, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetWorkflowRunViewResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetWorkflowRunViewResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.conversationId = UUID.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.currentNodeId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.revision = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.lastMessageId = UUID.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.messages.push(ConversationMessage.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.pendingInput = PendingInput.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetWorkflowRunViewResponse {
    return {
      conversationId: isSet(object.conversationId)
        ? UUID.fromJSON(object.conversationId)
        : isSet(object.conversation_id)
        ? UUID.fromJSON(object.conversation_id)
        : undefined,
      status: isSet(object.status) ? workflowRunStatusFromJSON(object.status) : 0,
      currentNodeId: isSet(object.currentNodeId)
        ? globalThis.String(object.currentNodeId)
        : isSet(object.current_node_id)
        ? globalThis.String(object.current_node_id)
        : "",
      revision: isSet(object.revision) ? globalThis.Number(object.revision) : 0,
      lastMessageId: isSet(object.lastMessageId)
        ? UUID.fromJSON(object.lastMessageId)
        : isSet(object.last_message_id)
        ? UUID.fromJSON(object.last_message_id)
        : undefined,
      messages: globalThis.Array.isArray(object?.messages)
        ? object.messages.map((e: any) => ConversationMessage.fromJSON(e))
        : [],
      pendingInput: isSet(object.pendingInput)
        ? PendingInput.fromJSON(object.pendingInput)
        : isSet(object.pending_input)
        ? PendingInput.fromJSON(object.pending_input)
        : undefined,
    };
  },

  toJSON(message: GetWorkflowRunViewResponse): unknown {
    const obj: any = {};
    if (message.conversationId !== undefined) {
      obj.conversationId = UUID.toJSON(message.conversationId);
    }
    if (message.status !== 0) {
      obj.status = workflowRunStatusToJSON(message.status);
    }
    if (message.currentNodeId !== "") {
      obj.currentNodeId = message.currentNodeId;
    }
    if (message.revision !== 0) {
      obj.revision = Math.round(message.revision);
    }
    if (message.lastMessageId !== undefined) {
      obj.lastMessageId = UUID.toJSON(message.lastMessageId);
    }
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => ConversationMessage.toJSON(e));
    }
    if (message.pendingInput !== undefined) {
      obj.pendingInput = PendingInput.toJSON(message.pendingInput);
    }
    return obj;
  },

  create(base?: DeepPartial<GetWorkflowRunViewResponse>): GetWorkflowRunViewResponse {
    return GetWorkflowRunViewResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetWorkflowRunViewResponse>): GetWorkflowRunViewResponse {
    const message = createBaseGetWorkflowRunViewResponse();
    message.conversationId = (object.conversationId !== undefined && object.conversationId !== null)
      ? UUID.fromPartial(object.conversationId)
      : undefined;
    message.status = object.status ?? 0;
    message.currentNodeId = object.currentNodeId ?? "";
    message.revision = object.revision ?? 0;
    message.lastMessageId = (object.lastMessageId !== undefined && object.lastMessageId !== null)
      ? UUID.fromPartial(object.lastMessageId)
      : undefined;
    message.messages = object.messages?.map((e) => ConversationMessage.fromPartial(e)) || [];
    message.pendingInput = (object.pendingInput !== undefined && object.pendingInput !== null)
      ? PendingInput.fromPartial(object.pendingInput)
      : undefined;
    return message;
  },
};

function createBaseSelectOptionAnswer(): SelectOptionAnswer {
  return { optionId: undefined, rawInput: undefined };
}

export const SelectOptionAnswer: MessageFns<SelectOptionAnswer> = {
  encode(message: SelectOptionAnswer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.optionId !== undefined) {
      writer.uint32(10).string(message.optionId);
    }
    if (message.rawInput !== undefined) {
      writer.uint32(18).string(message.rawInput);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SelectOptionAnswer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSelectOptionAnswer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.optionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rawInput = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SelectOptionAnswer {
    return {
      optionId: isSet(object.optionId)
        ? globalThis.String(object.optionId)
        : isSet(object.option_id)
        ? globalThis.String(object.option_id)
        : undefined,
      rawInput: isSet(object.rawInput)
        ? globalThis.String(object.rawInput)
        : isSet(object.raw_input)
        ? globalThis.String(object.raw_input)
        : undefined,
    };
  },

  toJSON(message: SelectOptionAnswer): unknown {
    const obj: any = {};
    if (message.optionId !== undefined) {
      obj.optionId = message.optionId;
    }
    if (message.rawInput !== undefined) {
      obj.rawInput = message.rawInput;
    }
    return obj;
  },

  create(base?: DeepPartial<SelectOptionAnswer>): SelectOptionAnswer {
    return SelectOptionAnswer.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SelectOptionAnswer>): SelectOptionAnswer {
    const message = createBaseSelectOptionAnswer();
    message.optionId = object.optionId ?? undefined;
    message.rawInput = object.rawInput ?? undefined;
    return message;
  },
};

function createBaseSubmitAnswerRequest(): SubmitAnswerRequest {
  return { workflowRunId: undefined, selectOption: undefined };
}

export const SubmitAnswerRequest: MessageFns<SubmitAnswerRequest> = {
  encode(message: SubmitAnswerRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workflowRunId !== undefined) {
      UUID.encode(message.workflowRunId, writer.uint32(10).fork()).join();
    }
    if (message.selectOption !== undefined) {
      SelectOptionAnswer.encode(message.selectOption, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubmitAnswerRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubmitAnswerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowRunId = UUID.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.selectOption = SelectOptionAnswer.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubmitAnswerRequest {
    return {
      workflowRunId: isSet(object.workflowRunId)
        ? UUID.fromJSON(object.workflowRunId)
        : isSet(object.workflow_run_id)
        ? UUID.fromJSON(object.workflow_run_id)
        : undefined,
      selectOption: isSet(object.selectOption)
        ? SelectOptionAnswer.fromJSON(object.selectOption)
        : isSet(object.select_option)
        ? SelectOptionAnswer.fromJSON(object.select_option)
        : undefined,
    };
  },

  toJSON(message: SubmitAnswerRequest): unknown {
    const obj: any = {};
    if (message.workflowRunId !== undefined) {
      obj.workflowRunId = UUID.toJSON(message.workflowRunId);
    }
    if (message.selectOption !== undefined) {
      obj.selectOption = SelectOptionAnswer.toJSON(message.selectOption);
    }
    return obj;
  },

  create(base?: DeepPartial<SubmitAnswerRequest>): SubmitAnswerRequest {
    return SubmitAnswerRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SubmitAnswerRequest>): SubmitAnswerRequest {
    const message = createBaseSubmitAnswerRequest();
    message.workflowRunId = (object.workflowRunId !== undefined && object.workflowRunId !== null)
      ? UUID.fromPartial(object.workflowRunId)
      : undefined;
    message.selectOption = (object.selectOption !== undefined && object.selectOption !== null)
      ? SelectOptionAnswer.fromPartial(object.selectOption)
      : undefined;
    return message;
  },
};

function createBaseSubmitAnswerResponse(): SubmitAnswerResponse {
  return {};
}

export const SubmitAnswerResponse: MessageFns<SubmitAnswerResponse> = {
  encode(_: SubmitAnswerResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubmitAnswerResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubmitAnswerResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): SubmitAnswerResponse {
    return {};
  },

  toJSON(_: SubmitAnswerResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<SubmitAnswerResponse>): SubmitAnswerResponse {
    return SubmitAnswerResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<SubmitAnswerResponse>): SubmitAnswerResponse {
    const message = createBaseSubmitAnswerResponse();
    return message;
  },
};

export type WorkflowsServiceDefinition = typeof WorkflowsServiceDefinition;
export const WorkflowsServiceDefinition = {
  name: "WorkflowsService",
  fullName: "platformik.workflows.v1.WorkflowsService",
  methods: {
    listWorkflows: {
      name: "ListWorkflows",
      requestType: ListWorkflowsRequest as typeof ListWorkflowsRequest,
      requestStream: false,
      responseType: ListWorkflowsResponse as typeof ListWorkflowsResponse,
      responseStream: false,
      options: {},
    },
    startWorkflow: {
      name: "StartWorkflow",
      requestType: StartWorkflowRequest as typeof StartWorkflowRequest,
      requestStream: false,
      responseType: StartWorkflowResponse as typeof StartWorkflowResponse,
      responseStream: false,
      options: {},
    },
    getWorkflowRunView: {
      name: "GetWorkflowRunView",
      requestType: GetWorkflowRunViewRequest as typeof GetWorkflowRunViewRequest,
      requestStream: false,
      responseType: GetWorkflowRunViewResponse as typeof GetWorkflowRunViewResponse,
      responseStream: false,
      options: {},
    },
    submitAnswer: {
      name: "SubmitAnswer",
      requestType: SubmitAnswerRequest as typeof SubmitAnswerRequest,
      requestStream: false,
      responseType: SubmitAnswerResponse as typeof SubmitAnswerResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface WorkflowsServiceImplementation<CallContextExt = {}> {
  listWorkflows(
    request: ListWorkflowsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListWorkflowsResponse>>;
  startWorkflow(
    request: StartWorkflowRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<StartWorkflowResponse>>;
  getWorkflowRunView(
    request: GetWorkflowRunViewRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetWorkflowRunViewResponse>>;
  submitAnswer(
    request: SubmitAnswerRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<SubmitAnswerResponse>>;
}

export interface WorkflowsServiceClient<CallOptionsExt = {}> {
  listWorkflows(
    request: DeepPartial<ListWorkflowsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListWorkflowsResponse>;
  startWorkflow(
    request: DeepPartial<StartWorkflowRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<StartWorkflowResponse>;
  getWorkflowRunView(
    request: DeepPartial<GetWorkflowRunViewRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetWorkflowRunViewResponse>;
  submitAnswer(
    request: DeepPartial<SubmitAnswerRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<SubmitAnswerResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
