// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v7.34.0
// source: auth_context.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'

export const protobufPackage = 'platformik.auth.v1'

export interface ServiceActor {
  service: string
}

export interface UserSubject {
  userId: string
  email: string
  emailVerified: boolean
}

export interface AuthContext {
  actor: ServiceActor | undefined
  subject?: UserSubject | undefined
}

function createBaseServiceActor(): ServiceActor {
  return { service: '' }
}

export const ServiceActor: MessageFns<ServiceActor> = {
  encode(message: ServiceActor, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.service !== '') {
      writer.uint32(10).string(message.service)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceActor {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseServiceActor()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.service = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): ServiceActor {
    return { service: isSet(object.service) ? globalThis.String(object.service) : '' }
  },

  toJSON(message: ServiceActor): unknown {
    const obj: any = {}
    if (message.service !== '') {
      obj.service = message.service
    }
    return obj
  },

  create(base?: DeepPartial<ServiceActor>): ServiceActor {
    return ServiceActor.fromPartial(base ?? {})
  },
  fromPartial(object: DeepPartial<ServiceActor>): ServiceActor {
    const message = createBaseServiceActor()
    message.service = object.service ?? ''
    return message
  },
}

function createBaseUserSubject(): UserSubject {
  return { userId: '', email: '', emailVerified: false }
}

export const UserSubject: MessageFns<UserSubject> = {
  encode(message: UserSubject, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== '') {
      writer.uint32(10).string(message.userId)
    }
    if (message.email !== '') {
      writer.uint32(18).string(message.email)
    }
    if (message.emailVerified !== false) {
      writer.uint32(24).bool(message.emailVerified)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserSubject {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseUserSubject()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.userId = reader.string()
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.email = reader.string()
          continue
        }
        case 3: {
          if (tag !== 24) {
            break
          }

          message.emailVerified = reader.bool()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): UserSubject {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
          ? globalThis.String(object.user_id)
          : '',
      email: isSet(object.email) ? globalThis.String(object.email) : '',
      emailVerified: isSet(object.emailVerified)
        ? globalThis.Boolean(object.emailVerified)
        : isSet(object.email_verified)
          ? globalThis.Boolean(object.email_verified)
          : false,
    }
  },

  toJSON(message: UserSubject): unknown {
    const obj: any = {}
    if (message.userId !== '') {
      obj.userId = message.userId
    }
    if (message.email !== '') {
      obj.email = message.email
    }
    if (message.emailVerified !== false) {
      obj.emailVerified = message.emailVerified
    }
    return obj
  },

  create(base?: DeepPartial<UserSubject>): UserSubject {
    return UserSubject.fromPartial(base ?? {})
  },
  fromPartial(object: DeepPartial<UserSubject>): UserSubject {
    const message = createBaseUserSubject()
    message.userId = object.userId ?? ''
    message.email = object.email ?? ''
    message.emailVerified = object.emailVerified ?? false
    return message
  },
}

function createBaseAuthContext(): AuthContext {
  return { actor: undefined, subject: undefined }
}

export const AuthContext: MessageFns<AuthContext> = {
  encode(message: AuthContext, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.actor !== undefined) {
      ServiceActor.encode(message.actor, writer.uint32(10).fork()).join()
    }
    if (message.subject !== undefined) {
      UserSubject.encode(message.subject, writer.uint32(18).fork()).join()
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthContext {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseAuthContext()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.actor = ServiceActor.decode(reader, reader.uint32())
          continue
        }
        case 2: {
          if (tag !== 18) {
            break
          }

          message.subject = UserSubject.decode(reader, reader.uint32())
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): AuthContext {
    return {
      actor: isSet(object.actor) ? ServiceActor.fromJSON(object.actor) : undefined,
      subject: isSet(object.subject) ? UserSubject.fromJSON(object.subject) : undefined,
    }
  },

  toJSON(message: AuthContext): unknown {
    const obj: any = {}
    if (message.actor !== undefined) {
      obj.actor = ServiceActor.toJSON(message.actor)
    }
    if (message.subject !== undefined) {
      obj.subject = UserSubject.toJSON(message.subject)
    }
    return obj
  },

  create(base?: DeepPartial<AuthContext>): AuthContext {
    return AuthContext.fromPartial(base ?? {})
  },
  fromPartial(object: DeepPartial<AuthContext>): AuthContext {
    const message = createBaseAuthContext()
    message.actor =
      object.actor !== undefined && object.actor !== null ? ServiceActor.fromPartial(object.actor) : undefined
    message.subject =
      object.subject !== undefined && object.subject !== null
        ? UserSubject.fromPartial(object.subject)
        : undefined
    return message
  },
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create(base?: DeepPartial<T>): T
  fromPartial(object: DeepPartial<T>): T
}
