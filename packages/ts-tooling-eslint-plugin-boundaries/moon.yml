$schema: 'https://moonrepo.dev/schemas/project.json'

tasks:
  typecheck:
    command: 'echo "typecheck: skipped (tooling package scaffold)"'
  test:
    command: 'echo "test: skipped (tooling package scaffold)"'
  format:
    command: 'echo "format: skipped (tooling package scaffold)"'
  format-fix:
    command: 'echo "format-fix: skipped (tooling package scaffold)"'
