// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v7.34.0
// source: primitives.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire'

export const protobufPackage = 'platformik.primitives.v1'

/**
 * UUID represents a UUIDv7 identifier.
 * The value is a canonical lowercase hyphenated string: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
 */
export interface UUID {
  value: string
}

function createBaseUUID(): UUID {
  return { value: '' }
}

export const UUID: MessageFns<UUID> = {
  encode(message: UUID, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== '') {
      writer.uint32(10).string(message.value)
    }
    return writer
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UUID {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input)
    const end = length === undefined ? reader.len : reader.pos + length
    const message = createBaseUUID()
    while (reader.pos < end) {
      const tag = reader.uint32()
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break
          }

          message.value = reader.string()
          continue
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break
      }
      reader.skip(tag & 7)
    }
    return message
  },

  fromJSON(object: any): UUID {
    return { value: isSet(object.value) ? globalThis.String(object.value) : '' }
  },

  toJSON(message: UUID): unknown {
    const obj: any = {}
    if (message.value !== '') {
      obj.value = message.value
    }
    return obj
  },

  create(base?: DeepPartial<UUID>): UUID {
    return UUID.fromPartial(base ?? {})
  },
  fromPartial(object: DeepPartial<UUID>): UUID {
    const message = createBaseUUID()
    message.value = object.value ?? ''
    return message
  },
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>

function isSet(value: any): boolean {
  return value !== null && value !== undefined
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter
  decode(input: BinaryReader | Uint8Array, length?: number): T
  fromJSON(object: any): T
  toJSON(message: T): unknown
  create(base?: DeepPartial<T>): T
  fromPartial(object: DeepPartial<T>): T
}
