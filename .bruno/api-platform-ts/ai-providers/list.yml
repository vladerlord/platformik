info:
  name: list
  type: http
  seq: 1

http:
  method: GET
  url: '{{baseUrl}}/ai-providers'
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
