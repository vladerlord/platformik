info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: '{{baseUrl}}/api/v1/auth/login'
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {
        "email": "{{email}}",
        "password": "{{password}}"
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
