info:
  name: get-run-view
  type: http
  seq: 4

http:
  method: GET
  url: '{{baseUrl}}/api/v1/workflows/runs/{{workflowRunId}}'
  headers:
    - name: Content-Type
      value: application/json
  auth: bearer

auth:
  bearer:
    token: '{{sessionToken}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
