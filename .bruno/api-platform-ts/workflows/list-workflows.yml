info:
  name: list-workflows
  type: http
  seq: 2

http:
  method: GET
  url: '{{baseUrl}}/api/v1/workflows'
  headers:
    - name: Content-Type
      value: application/json
  auth: bearer

auth:
  bearer:
    token: '{{sessionToken}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
