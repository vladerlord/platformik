info:
  name: events-sse
  type: http
  seq: 6

http:
  method: GET
  url: '{{baseUrl}}/api/v1/workflows/runs/{{workflowRunId}}/events'
  headers:
    - name: Accept
      value: text/event-stream
  auth: bearer

auth:
  bearer:
    token: '{{sessionToken}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
